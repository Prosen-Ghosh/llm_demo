# Server
ENV=development
HOST=0.0.0.0
PORT=8000
WORKERS=1

# Concurrency
MAX_CONCURRENCY=4

# OpenRouter / upstream LLM proxy (example)
OPENROUTER_BASE_URL=https://api.openrouter.ai
OPENROUTER_API_KEY=replace_me_with_real_key

# Upstream timeouts (seconds)
UPSTREAM_TIMEOUT=60
UPSTREAM_STREAM_TIMEOUT=120

# Backoff
MAX_RETRIES=3
BACKOFF_BASE=0.4
