# concurrency: maximum concurrent enrichment calls
MAX_CONCURRENCY=8

# external enrichment API (simulate with httpbin.org or a mock local endpoint)
EXTERNAL_API_URL=http://localhost:8000/dummy-enrich

# httpx timeout (seconds)
EXTERNAL_API_TIMEOUT=5

# retries for external API
MAX_RETRIES=3

# base waiting seconds for exponential backoff
BACKOFF_BASE=0.5

# environment
ENV=development
